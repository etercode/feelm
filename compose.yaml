services:
  php:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
      target: dev
    container_name: youknowme_php
    depends_on:
      database:
        condition: service_healthy
    restart: unless-stopped

  nginx:
    build:
      context: .
      dockerfile: docker/nginx/Dockerfile
    container_name: youknowme_nginx
    depends_on:
      - php
    restart: unless-stopped

  database:
    image: postgres:${POSTGRES_VERSION:-18}-alpine
    container_name: youknowme_db
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-youknowme}
      POSTGRES_USER: ${POSTGRES_USER:-app}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-app}
      # A stable data path inside the volume, across image versions.
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - database_data:/var/lib/postgresql/data:rw
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-app} -d ${POSTGRES_DB:-youknowme}']
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

volumes:
  database_data:
