doctrine:
    dbal:
        url: '%env(resolve:DATABASE_URL)%'

        # IMPORTANT: You MUST configure your server version,
        # either here or in the DATABASE_URL env var (see .env file)
        #server_version: '16'

        profiling_collect_backtrace: '%kernel.debug%'

        # The full-text column on works is a Postgres tsvector. Mapping it to a
        # string keeps schema comparisons quiet about a column no entity owns.
        mapping_types:
            tsvector: string

        # The tmdb_*_ids tables are crawl queues owned entirely by migrations —
        # no entity, and partial indexes DBAL cannot describe. Hiding them from
        # schema comparison keeps `doctrine:schema:validate` meaningful.
        #
        # schema_filter matches table names only, which is why the one index
        # below could not be hidden the same way.
        schema_filter: '~^(?!tmdb_(movie|series)_ids$).*~'

        # KNOWN DRIFT, and the only one: `doctrine:schema:validate` reports
        #
        #     DROP INDEX idx_work_search_popular;
        #
        # It is not drift. The index is created by Version20260803120000 and is
        # load-bearing for search — see WorkSearch::pooled(). It cannot be
        # declared on the entity because it indexes search_vector, a generated
        # column no entity maps, and Doctrine refuses an index on a column it
        # does not know. Mapping the column instead would pull a tsvector into
        # every hydration, which is worse than this note.
        #
        # So: mapping errors are real, and exactly one schema difference is
        # expected. A second one is not.
    orm:
        validate_xml_mapping: true
        naming_strategy: doctrine.orm.naming_strategy.underscore
        identity_generation_preferences:
            Doctrine\DBAL\Platforms\PostgreSQLPlatform: identity
        auto_mapping: true
        mappings:
            App:
                type: attribute
                is_bundle: false
                dir: '%kernel.project_dir%/src/Entity'
                prefix: 'App\Entity'
                alias: App

when@test:
    doctrine:
        dbal:
            # "TEST_TOKEN" is typically set by ParaTest
            dbname_suffix: '_test%env(default::TEST_TOKEN)%'

when@prod:
    doctrine:
        orm:
            query_cache_driver:
                type: pool
                pool: doctrine.system_cache_pool
            result_cache_driver:
                type: pool
                pool: doctrine.result_cache_pool

    framework:
        cache:
            pools:
                doctrine.result_cache_pool:
                    adapter: cache.app
                doctrine.system_cache_pool:
                    adapter: cache.system
